{% set name = "lisc" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/lisc-tools/lisc/archive/{{ version }}.tar.gz
  sha256: 8b7b4b6d3d391bcc2479f195d87043b32d71f1c252ccfd65ce2ea9ec49c84fd9

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.5
    - pip
  run:
    - python >=3.5
    - numpy
    - requests
    - lxml
    - beautifulsoup4
    - nltk

test:
  imports:
    - lisc
    - lisc.analysis
    - lisc.collect
    - lisc.core
    - lisc.data
    - lisc.objects
    - lisc.plts
    - lisc.requester
    - lisc.tests
    - lisc.urls
    - lisc.utils

about:
  home: https://github.com/lisc-tools/lisc
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: LISC is a package for collecting and analyzing the scientific literature.
  doc_url: https://lisc-tools.github.io/lisc/
  dev_url: https://github.com/lisc-tools/lisc

extra:
  recipe-maintainers:
    - ryanhammonds
